AWSTemplateFormatVersion: '2010-09-09'
Description: >
  IoT Monitoring Platform (Project 3b) — DynamoDB Devices table and
  least-privilege IAM user for the device gateway.

Parameters:
  Environment:
    Type: String
    Default: prod
    AllowedValues: [prod, staging]
    Description: Deployment environment prefix for table names

Resources:

  # ---------------------------------------------------------------------------
  # DynamoDB Table
  # ---------------------------------------------------------------------------

  DevicesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub 'p3-${Environment}-Devices'
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: device_id
          AttributeType: S
      KeySchema:
        - AttributeName: device_id
          KeyType: HASH
      Tags:
        - Key: Project
          Value: iot-monitoring-platform
        - Key: Environment
          Value: !Ref Environment

  # ---------------------------------------------------------------------------
  # IAM User (least privilege — only what the gateway needs)
  # ---------------------------------------------------------------------------

  GatewayUser:
    Type: AWS::IAM::User
    Properties:
      UserName: iot-gateway-app
      Tags:
        - Key: Project
          Value: iot-monitoring-platform

  GatewayUserPolicy:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: iot-gateway-app-dynamodb
      Users:
        - !Ref GatewayUser
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Action:
              - dynamodb:GetItem
              - dynamodb:PutItem
              - dynamodb:UpdateItem
            Resource:
              - !GetAtt DevicesTable.Arn

  # ---------------------------------------------------------------------------
  # IAM User for the consumer (3b-2) — writes the shared contract tables owned
  # by project 1b; deliberately a separate user (least privilege: the gateway
  # can only touch Devices, the consumer only the contract tables)
  # ---------------------------------------------------------------------------

  ConsumerUser:
    Type: AWS::IAM::User
    Properties:
      UserName: iot-gateway-consumer
      Tags:
        - Key: Project
          Value: iot-monitoring-platform

  ConsumerUserPolicy:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: iot-gateway-consumer-dynamodb
      Users:
        - !Ref ConsumerUser
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Action:
              - dynamodb:PutItem
            Resource:
              - !Sub 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${Environment}-SensorEvents'
          - Effect: Allow
            Action:
              - dynamodb:GetItem
              - dynamodb:PutItem
            Resource:
              - !Sub 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${Environment}-RoomStatus'

# ---------------------------------------------------------------------------
# Outputs
# ---------------------------------------------------------------------------

Outputs:
  DevicesTableName:
    Description: DynamoDB table name for registered devices
    Value: !Ref DevicesTable

  GatewayUserArn:
    Description: IAM user ARN — create access key manually after deployment
    Value: !GetAtt GatewayUser.Arn

  ConsumerUserArn:
    Description: IAM user ARN for the normalizer consumer — create access key manually
    Value: !GetAtt ConsumerUser.Arn
