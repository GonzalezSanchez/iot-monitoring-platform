name: lakehouse_dbt
version: "1.0.0"
config-version: 2

profile: lakehouse_dbt

model-paths: ["models"]
test-paths: ["tests"]
log-path: "logs"
target-path: "target"

models:
  lakehouse_dbt:
    staging:
      +materialized: view
      +schema: silver
    intermediate:
      +materialized: ephemeral
    marts:
      +materialized: incremental
      +schema: gold
