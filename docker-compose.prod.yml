services:
  backend:
    image: ghcr.io/gonzalezsanchez/iot-backend:latest
    env_file:
      - ./backend/project1b-smart-room-monitor-fastapi/.env.prod
    restart: unless-stopped
    expose:
      - "8000"

  frontend:
    image: ghcr.io/gonzalezsanchez/iot-frontend:latest
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
